"""Substitution (`subs`) and high-precision numeric evaluation.

Planned contents:

- `subs`: replace `Symbol` nodes with expressions or Decimo `BigDecimal` values
- `evalf`: evaluate any expression tree to a Decimo `BigDecimal` at a requested
  precision, dispatching elementary functions to their Decimo implementations

TODO: implement. This is a placeholder.
"""

# from decimo import BigDecimal
